---

- name: "Output test results"
  debug:
    var: "{{ SalesForceBundle }}"

- name: "Wait for test results"
  shell: "uptime"
  until: IntegrationBundle is defined and IntegrationBundle !== ""
  retries: 10
  delay: 10